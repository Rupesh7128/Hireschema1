/**
 * Optional page helper — primary save path uses popup + executeScript.
 * Exposes a small toast when a future in-page button is used.
 */
(function () {
  // Reserved for in-page "Save to Hireschema" button in a later iteration.
})();
