/** Same-origin URLs — the product SPA hosts landing + app on one Vercel deploy. */
export const SITE_URL = "https://hireschema.com";
/** Empty: links are relative so they stay on the app, not a second origin. */
export const APP_URL = "";
export const REVIEW_CV_URL = "/reviewmycv";
export const INVITE_URL = "/invite";
export const SIGNIN_URL = "/signup?mode=signin";
export const UPDATED = "18 August 2026";
